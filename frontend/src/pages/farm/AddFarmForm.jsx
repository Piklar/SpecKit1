import { Container, Typography } from '@mui/material';
export default function AddFarmForm() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Add a new farm</Typography>
    </Container>
  );
}
