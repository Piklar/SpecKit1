import { Container, Typography } from '@mui/material';
export default function CalendarPage() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Crop Calendar</Typography>
      <Typography>Calendar component placeholder</Typography>
    </Container>
  );
}
