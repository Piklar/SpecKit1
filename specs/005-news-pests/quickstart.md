# Quickstart

1. Check out branch `005`.
2. Ensure MongoDB is running.
3. Start the backend: `npm run dev` in `/backend`.
4. Start the frontend: `npm run dev` in `/frontend`.
5. Navigate to `/news` and `/pests` to see the new pages.
6. Verify the calendar bug is fixed by adding a task to a date and clicking it.
